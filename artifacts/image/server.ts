export const imageArtifactServer = {
  type: 'image',
  name: 'Image',
  handler: () => ({ success: true }),
};

export const imageDocumentHandler = {
  type: 'image',
  handler: () => ({ success: true }),
};