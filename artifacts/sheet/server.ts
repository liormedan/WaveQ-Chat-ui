export const sheetArtifactServer = {
  type: 'sheet',
  name: 'Sheet',
  handler: () => ({ success: true }),
};

export const sheetDocumentHandler = {
  type: 'sheet',
  handler: () => ({ success: true }),
};