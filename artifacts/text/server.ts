export const textArtifactServer = {
  type: 'text',
  name: 'Text',
  handler: () => ({ success: true }),
};

export const textDocumentHandler = {
  type: 'text',
  handler: () => ({ success: true }),
};